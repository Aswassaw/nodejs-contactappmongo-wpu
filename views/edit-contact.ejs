<div class="card" style="max-width: 600px;">
    <div class="card-body">
        <div class="row">
            <div>
                <h2>Form Edit Data Contact</h2>
                <hr>

                <% if (typeof errors != 'undefined') { %>
                    <div class="alert alert-danger">
                        <ul>
                            <% errors.forEach(err => { %>
                                <li><%= err.msg %></li>
                            <% }) %>
                        </ul>
                    </div>
                <% } %>

                <form method="post" action="/contact/<%= contact._id %>?_method=PUT">
                    <div class="mb-1">
                        <label for="nama">Nama</label>
                        <input type="text" name="nama" id="nama" value="<%= contact.nama %>" class="form-control" required>
                    </div>
                    <div class="mb-1">
                        <label for="email">Email</label>
                        <input type="email" name="email" id="email" value="<%= contact.email %>" class="form-control" required>
                    </div>
                    <div class="mb-1">
                        <label for="nohp">No HP</label>
                        <input type="text" name="nohp" id="nohp" value="<%= contact.nohp %>" class="form-control" required>
                    </div>

                    <hr>
                    <input class="btn btn-primary" type="submit" value="Ubah Data">
                </form>
            </div>
        </div>
    </div>
</div>